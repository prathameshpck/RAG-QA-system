This file contains notes for the front-end web application. 
This is made using react. Apparently, react compiles its components so using multistage builds is pretty advantageous here 
Basically react is a high level JS framework that writes low level HTML/CSS/JS. And since its all html/css/js at the end of it, we don't need the react framework code at all, just the output of it. Current version of the code just has a dummy API call. Will fill this later



